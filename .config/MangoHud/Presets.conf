[preset 1]
# Just the FPS number
legacy_layout=false
fps
# Hides the "FPS" label, leaving just the number
fps_only
font_size=24
font_scale=1.5
text_color=FFFFFF
background_alpha=0.0

[preset 2]
# Horizontal bar style
horizontal
background_alpha=0.1
font_scale=1.2
gpu_stats
gpu_temp
gpu_core_clock
cpu_stats
cpu_temp
cpu_mhz
ram
vram
fps
frame_timing

[preset 3]
# Full Stats (Medium)
legacy_layout=false
gpu_stats
gpu_temp
gpu_core_clock
gpu_power
cpu_stats
cpu_temp
cpu_mhz
cpu_power
ram
vram
fps
fps_metrics=avg,0.01
frame_timing

[preset 4]
# "Nuclear" Info (Compact)
hud_compact
gpu_stats
gpu_temp
gpu_junction_temp
gpu_core_clock
gpu_fan
gpu_power
cpu_stats
cpu_temp
cpu_mhz
cpu_power
io_stats
ram
vram
fps
fps_metrics=avg,0.01
wine
gamemode
